#include <iostream>
using namespace std;

//Declare um array de caracteres frase com 20 posições.
//Leia uma frase do usuário usando cin e armazene-a em frase. Substitua todas as vogais da frase por '*'. 
//Imprima a frase com as vogais substituídas usando cout.

int main(){
    char frase[20];
    cout << "Digite uma frase: " << endl;
    cin >> frase;

    if(frase[0] = 'a', 'e', 'i', 'o', 'u'){
        frase[0] = '*';
        cout << frase << endl;
    }
    if(frase[1] = 'a', 'e', 'i', 'o', 'u'){
        frase[1] = '*';
        cout << frase << endl;
    }
    if(frase[2] = 'a', 'e', 'i', 'o', 'u'){
        frase[2] = '*';
        cout << frase << endl;
    }
    if(frase[3] = 'a', 'e', 'i', 'o', 'u'){
        frase[3] = '*';
        cout << frase << endl;
    }
    if(frase[4] = 'a', 'e', 'i', 'o', 'u'){
        frase[4] = '*';
        cout << frase << endl;
    }
    if(frase[5] = 'a', 'e', 'i', 'o', 'u'){
        frase[5] = '*';
        cout << frase << endl;
    }
    if(frase[6] = 'a', 'e', 'i', 'o', 'u'){
        frase[6] = '*';
        cout << frase << endl;
    }
    if(frase[7] = 'a', 'e', 'i', 'o', 'u'){
        frase[7] = '*';
        cout << frase << endl;
    }
    if(frase[8] = 'a', 'e', 'i', 'o', 'u'){
        frase[8] = '*';
        cout << frase << endl;
    }
    if(frase[9] = 'a', 'e', 'i', 'o', 'u'){
        frase[9] = '*';
        cout << frase << endl;
    }
    if(frase[10] = 'a', 'e', 'i', 'o', 'u'){
        frase[10] = '*';
        cout << frase << endl;
    }
    if(frase[11] = 'a', 'e', 'i', 'o', 'u'){
        frase[11] = '*';
        cout << frase << endl;
    }
    if(frase[12] = 'a', 'e', 'i', 'o', 'u'){
        frase[12] = '*';
        cout << frase << endl;

    if(frase[13] = 'a', 'e', 'i', 'o', 'u'){
        frase[13] = '*';
        cout << frase << endl;
    }
    if(frase[13] = 'a', 'e', 'i', 'o', 'u'){
        frase[13] = '*';
        cout << frase << endl;
    }
    if(frase[14] = 'a', 'e', 'i', 'o', 'u'){
        frase[14] = '*';
        cout << frase << endl;
    }
    if(frase[15] = 'a', 'e', 'i', 'o', 'u'){
        frase[15] = '*';
        cout << frase << endl;
    }
    if(frase[16] = 'a', 'e', 'i', 'o', 'u'){
        frase[16] = '*';
        cout << frase << endl;
    }
    if(frase[17] = 'a', 'e', 'i', 'o', 'u'){
        frase[17] = '*';
        cout << frase << endl;
    }
    if(frase[18] = 'a', 'e', 'i', 'o', 'u'){
        frase[18] = '*';
        cout << frase << endl;
    }
    if(frase[19] = 'a', 'e', 'i', 'o', 'u'){
        frase[19] = '*';
        cout << frase << endl;
    }
    }
    return false;
}