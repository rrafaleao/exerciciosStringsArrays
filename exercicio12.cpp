#include <iostream>
#include <string>
#include <stdlib.h>
#include <string.h>
using namespace std;

int main(){
    char frase[20];
    int tamanho; 
    tamanho = 'a' || 'e' || 'i' || 'o' || 'u';
    tamanho = strlen(frase);
    cout<<"Digite uma frase: " << endl;
    cin >> frase;
    cout << "tamnha da frase: " << tamanho << endl;
    }
